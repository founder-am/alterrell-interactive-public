# Alterrell Interactive — Netlify redirects
# Clean URLs: strip trailing slashes, handle common patterns

# Revived by AMA 2026-08-16: both now point at the live Astro route, so the
# legacy sodium URLs recover their inbound links instead of falling through
# to the catch-all.
/fast-food-sodium     /pieces/fast-food-sodium/     301
/sodium               /pieces/fast-food-sodium/     301

# Held: no target exists for either. Congress has not been built.
# /congress-salary      /congress-salary/      301
# /congress             /congress-salary/      301

# Slug renames — May 2026
# IMPORTANT: specific paths must precede the what-in-a-name/* catch-all
# so Netlify matches part-0 and part-2 before falling through to part-1
# Held until naming ships; the target will then be /pieces/naming/.
# /what-in-a-name/part-0/*  /naming/part-0/:splat  301
# /what-in-a-name/part-2/*  /naming/part-2/:splat  301
# /what-in-a-name/*         /naming/part-1/:splat  301
# Held: /gay-uncles/ has not migrated to Astro and is not in dist.
# /big-black-love/*         /gay-uncles/:splat     301

# Catch-all: anything not matched → the real 404 page, under a 404 status.
# This must point at /404.html, never at /index.html: sending the hub under
# a 404 status makes every mistyped URL look like the site works while
# telling crawlers it does not.
/*    /404.html    404
